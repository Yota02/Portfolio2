// URL de production (GitHub Pages). Sert à construire les URLs absolues
// nécessaires aux balises Open Graph / Twitter (les chemins relatifs ne
// fonctionnent pas pour les aperçus de partage).
export const SITE_URL = 'https://yota02.github.io/Portfolio2'

// Image de partage par défaut, utilisée quand une page n'en fournit pas.
export const DEFAULT_OG_IMAGE = `${SITE_URL}/projet/Victoria/logo.webp`
